# Subtitle presets saved from the adjust panel (Alt+a, then w).
#
# mpv.conf includes this file, so blocks here register as real profiles and
# the substyle script adds them to the end of the style cycle.
# Delete one with x in the adjust panel, or by removing its block by hand.
# Keep this file in place even when empty: mpv prints an error on every
# launch if an included file is missing.
